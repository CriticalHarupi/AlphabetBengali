name: Test (models)

on:
  push:
    paths:
      - 'backend/**'
  workflow_dispatch:

jobs:
  test:
    runs-on: go-test
    steps:
      - uses: actions/checkout@v6

      - name: Test
        working-directory: backend
        run: go test ./models/...
